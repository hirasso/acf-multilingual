{
  "name": "acf-multilingual",
  "author": "Rasso Hilber",
  "license": "GPL-2.0-or-later",
  "version": "3.2.1",
  "private": true,
  "main": "index.js",
  "type": "module",
  "scripts": {
    "preinstall": "git config core.hooksPath .githooks",
    "generate-pot": "node ./lang/generate-pot.js",
    "format": "pnpm run format:assets && pnpm run format:php",
    "format:assets": "pnpm exec prettier --write 'resources/*.{js,css}'",
    "format:php": "./vendor/bin/pint",
    "analyse": "pnpm run analyse:js && pnpm run analyse:php",
    "analyse:js": "pnpm exec tsc --noEmit --project ./jsconfig.json",
    "analyse:php": "vendor/bin/phpstan analyze --memory-limit=1G",
    "version": "pnpm changeset version && bin/cli.js version:patch",
    "install-wp-tests": "echo https://make.wordpress.org/cli/handbook/misc/plugin-unit-tests/#3-initialize-the-testing-environment-locally",
    "test:prepare": "composer install && bash bin/install-wp-tests.sh wordpress_test root 'root' localhost latest",
    "test:php": "WP_TESTS_DIR=~/Documents/wp-tests-dir/wordpress-tests-lib ./vendor/bin/phpunit",
    "wp-env": "wp-env",
    "wp-env:cli": "pnpm run wp-env run cli --env-cwd=wp-content/plugins/acf-multilingual",
    "wp-env:cli:tests": "pnpm run wp-env run tests-cli --env-cwd=wp-content/plugins/acf-multilingual",
    "wp-env:open": "open http://localhost:9783",
    "wp-env:open:tests": "open http://localhost:9784",
    "test": "pnpm test:pest",
    "test:pest": "pnpm run wp-env:cli:tests vendor/bin/pest --no-coverage",
    "test:pest:ci": "pnpm run wp-env:cli:tests vendor/bin/pest --coverage-clover coverage.xml"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "browserslist": [
    "> 1%",
    "not dead"
  ],
  "postcss": {
    "plugins": {
      "autoprefixer": true,
      "postcss-logical": true,
      "postcss-dir-pseudo-class": true
    }
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "@types/jquery": "^3.5.33",
    "@wordpress/env": "^10.34.0",
    "fast-glob": "^3.3.3",
    "lint-staged": "^16.2.6",
    "picocolors": "^1.1.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "wp-pot": "^1.10.2"
  }
}
